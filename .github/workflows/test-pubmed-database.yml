name: "test: pubmed-database"

on:
  push:
    branches: [main]
    paths:
      - "skills/pubmed-database/**"
  pull_request:
    branches: [main]
    paths:
      - "skills/pubmed-database/**"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: skills/pubmed-database/cli
        run: bun install

      - name: Run tests
        working-directory: skills/pubmed-database/cli
        run: bun test
