name: "test: jobdanmark-search"

on:
  push:
    branches: [main]
    paths:
      - "skills/jobdanmark-search/**"
  pull_request:
    branches: [main]
    paths:
      - "skills/jobdanmark-search/**"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: skills/jobdanmark-search/cli
        run: bun install

      - name: Run tests
        working-directory: skills/jobdanmark-search/cli
        run: bun test
